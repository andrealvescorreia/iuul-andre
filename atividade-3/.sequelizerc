const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'sequelize', 'config', 'config.js'),
  'models-path': resolve(__dirname, 'src', 'sequelize', 'models'),
  'migrations-path': resolve(__dirname, 'src', 'sequelize', 'migrations'),
  'seeders-path': resolve(__dirname, 'src', 'sequelize', 'seeds'),
}